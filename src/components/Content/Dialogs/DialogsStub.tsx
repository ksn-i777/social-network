import React from "react"
import s from './DialogsStub.module.css'

export const DialogsStub = React.memo(() => {
    return <div className={s.dialogs}>Dialogs: there are no endpoints corresponding to this section on the server</div>
})