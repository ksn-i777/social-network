import React from "react"
import s from './Settings.module.css'

export const Settings = React.memo(() => {
    return <div className={s.settings}>Settings: there are no endpoints corresponding to this section on the server</div>
})